# ICU Smart Monitoring — Backend (FastAPI)
# Python 3.11+

fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2
email-validator==2.2.0

# IA / calcul (traitement local temps réel)
numpy==1.26.4
pandas==2.2.1
scikit-learn==1.4.2
# torch : décommenter pour l'agent ECG ML (Phase 3)
# torch==2.3.1

# Sécurité / Auth
pyjwt==2.9.0
bcrypt==4.2.0

# Base de données (OPTIONNELLE — MVP fonctionne sans)
sqlalchemy==2.0.35
asyncpg==0.29.0
alembic==1.13.2

# Gemini (OPTIONNEL — jamais dans la boucle temps réel)
# google-generativeai==0.7.2

# Tests
pytest==8.3.3
httpx==0.27.2
